import React, { Component, createContext } from "react";

const readerContext = createContext();

export default class ReaderContext extends Component {
  state = {};
  render() {
    return <div>www</div>;
  }
}
